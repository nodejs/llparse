{
  "name": "llparse",
  "version": "3.0.0",
  "main": "lib/llparse.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf lib",
    "prepare": "npm run clean && npm run build",
    "lint": "tslint -c tslint.json src/*.ts src/**/*.ts src/**/**/*.ts src/**/**/**/*.ts test/*.ts",
    "mocha": "mocha -r ts-node/register/type-check --reporter spec test/*-test.ts",
    "test": "npm run mocha && npm run lint"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "llparse",
    "llvm",
    "ir",
    "dfa"
  ],
  "author": "Fedor Indutny <fedor@indutny.com> (http://darksi.de/)",
  "license": "MIT",
  "dependencies": {
    "bitcode": "^1.0.4",
    "debug": "^3.1.0"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.48",
    "@types/node": "^9.4.7",
    "async": "^2.6.0",
    "eslint": "^4.18.2",
    "llparse-test-fixture": "git://github.com/indutny/llparse-test-fixture.git",
    "mocha": "^5.0.4",
    "ts-node": "^5.0.1",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  },
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "repository": {
    "type": "git",
    "url": "gh:indutny/llparse"
  },
  "description": "Generating parsers in LLVM IR"
}
